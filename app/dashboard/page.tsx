export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-sm text-slate-500">
        Auth is wired up and this page is protected. Next up — Module 1: add
        Threads creators to track and pull their recent posts.
      </p>
    </div>
  );
}
